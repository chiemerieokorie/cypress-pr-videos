# dotenv-linter:off IncorrectDelimiter

# Do not commit your actual .env file to Git! This may contain secrets or other
# private information.

# Enable/disable step debug logging (default: `false`). For local debugging, it
# may be useful to set it to `true`.
ACTIONS_STEP_DEBUG=true

# GitHub Actions inputs should follow `INPUT_<name>` format (case-sensitive).
# Hyphens should not be converted to underscores!
INPUT_R2-ACCOUNT-ID=your-cloudflare-account-id
INPUT_R2-ACCESS-KEY-ID=your-r2-access-key-id
INPUT_R2-SECRET-ACCESS-KEY=your-r2-secret-access-key
INPUT_R2-BUCKET=ci-artifacts
INPUT_VIDEO-DIR=cypress/videos
INPUT_SPEC-PATTERN=cypress/e2e/**/*.cy.{ts,js,tsx,jsx}
INPUT_URL-EXPIRY-SECONDS=259200
INPUT_GITHUB-TOKEN=fake-token
INPUT_COMMENT-HEADER=### 🎬 Cypress Test Videos
INPUT_MAX-CONCURRENT-UPLOADS=5

# Required GitHub context for local testing
GITHUB_REPOSITORY=your-org/your-repo
GITHUB_EVENT_NAME=pull_request
GITHUB_EVENT_PATH=__fixtures__/pull_request_event.json
